<section
  class="relative w-full max-w-[1700px] mx-auto rounded-3xl
         min-h-[calc(100vh-3rem)]
         bg-gradient-to-br from-[#111827] to-[#020617]
         border border-white/10
         shadow-[0_40px_120px_rgba(0,0,0,0.65)]
         backdrop-blur-xl overflow-hidden
         flex flex-col"
>

  <!-- halos -->
  <div class="absolute -top-32 -right-32 w-96 h-96 bg-yellow-400/10 blur-3xl rounded-full"></div>
  <div class="absolute -bottom-32 -left-32 w-96 h-96 bg-yellow-300/10 blur-3xl rounded-full"></div>

  <!-- contenido con padding, y separando header del borde -->
    <div class="relative flex-1 px-8 md:px-16 pt-10 md:pt-12">
    <slot />
  </div>

</section>
